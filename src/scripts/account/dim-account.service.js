/**
 * There is one DIM account per user, per DIM stage (prod, beta,
 * dev). A user who uses different browsers and doesn't sync between
 * them will have different DIM accounts.
 *
 * This is essentially virtual - it's mostly represented by the settings service.
 */
function DimAccountService() {

}
