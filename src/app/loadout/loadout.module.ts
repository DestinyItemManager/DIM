import { module } from 'angular';

import { LoadoutDrawerComponent } from './loadout-drawer.component';

export default module('loadoutModule', []).component('loadoutDrawer', LoadoutDrawerComponent).name;
