// A single file for the settings components so we can load them as one chunk

export { default as SettingsPage } from './SettingsPage';
export { default as GDriveRevisions } from '../storage/GDriveRevisions';
export { default as AuditLog } from './AuditLog';
